/** @type {import('next').NextConfig} */
module.exports = {
  // Uncomment to verify that the `htmlLimitedBots` escape hatch only fixes the
  // Node.js runtime route and is still ignored by the Edge SSR route:
  // htmlLimitedBots: /GPTBot|ChatGPT-User|ClaudeBot|TelegramBot/.source,
}
