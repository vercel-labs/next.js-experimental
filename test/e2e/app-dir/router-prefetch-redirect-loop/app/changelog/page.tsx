export default function ChangelogPage() {
  // A static route whose tree has a different shape than the dynamic
  // `/[collection]/[...slug]` route that `/docs/changelog` matches.
  return <p id="changelog-page">changelog</p>
}
